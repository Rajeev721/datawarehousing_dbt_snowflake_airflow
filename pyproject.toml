[project]
name = "datawarehousing-dbt-snowflake-airflow"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Rajeev Suravajhula", email = "rajeev.suravajhula@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "dbt-core>=1.12.5",
    "dbt-snowflake>=1.12.1",
]

[project.scripts]
datawarehousing-dbt-snowflake-airflow = "datawarehousing_dbt_snowflake_airflow:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
